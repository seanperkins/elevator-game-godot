source "https://rubygems.org"

# Fastlane for the TestFlight / App Store release pipeline.
# Run via `bundle exec fastlane <lane>` (uses the project's Ruby, not Homebrew's
# fastlane, which bundles an unsupported Ruby 4.0.5).
gem "fastlane"
