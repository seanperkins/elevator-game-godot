I’ll verify whether the revised text is present in the review bundle so I can cite stable line numbers. I’ll also re-check the corrected paths specifically for save migration, catch-up accounting, and lifecycle re-entry; no files will be changed.The revisions address the earlier persistence probe, parse ordering, RNG continuation, import-error suppression, fixed-step rate, and Milestone 0 concerns. Remaining issues:

- **P0 — This still is not an implementation plan.** The document explicitly says “pending implementation plan” at [plan.md:5](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:5). It describes intended behavior but still lacks file-by-file changes, method signatures, ownership, error-return contracts, and command sequences. Several problems below arise because critical mechanics are described without an executable algorithm.

- **P1 — A one-minute integrator cannot match the 20 Hz simulation within `1e-9`.** Catch-up advances in 60-second steps at [plan.md:234](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:234), while the test demands `1e-9` relative agreement with the live simulation at [plan.md:458](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:458). Consider a rush-rate boundary at 08:55:30: a one-minute sample can apply either rate to the entire 08:55–08:56 interval, producing up to 30 seconds of traffic error. Capacity saturation, move-out thresholds, and integer passenger spawns make the divergence larger and discontinuous. Sharing the curve does not make different step sizes equivalent. Define an exact piecewise integration algorithm and realistic error bounds per output; integer rider counts especially cannot generally agree exactly with an aggregate model unless it reproduces the live RNG/events.

- **P1 — The stall reconciliation has overlapping authorities and can double-apply state.** Lines [333–336](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:333) send discarded accumulator time to catch-up while declaring wall time authoritative for earnings and ticks authoritative for state. But catch-up also evolves tenancy state at [plan.md:254](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:254). After a ten-second stall, it is undefined which interval the retained ticks cover, which interval catch-up covers, and whether deliveries/rent in the retained interval are included again by aggregate catch-up. Specify non-overlapping `[start, end)` intervals, the maximum live ticks processed, and a single reconciliation transaction for cash, passengers, tenancy, and timestamps.

- **P1 — “Single-flight” drops the most important save request.** A second save is blocked at [plan.md:411](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:411). If the 30-second timer begins saving, the player buys an upgrade, and then backgrounds Safari while that write is active, the visibility-triggered save is discarded. Killing the tab can restore the pre-purchase state even though §7.1 promises an immediate hidden-state save. A save requested during an active write must mark the state dirty and queue/coalesce one final snapshot after completion.

- **P1 — Catch-up lacks an atomic progress watermark.** Hidden state stamps `saved_at`, and resume computes from it at [plan.md:215](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:215), but the plan never says when catch-up advances and persists that timestamp. If a one-hour catch-up is applied and a later timer serializes the new cash with the old `saved_at`, the next reload awards the same hour again. Applying catch-up, advancing the watermark, and producing the resulting save snapshot must be one logical transaction. Define timestamp behavior for timer saves, hidden saves, successful catch-up, and failed writes.

- **P1 — Current-schema validation before migration rejects valid old saves.** The load sequence validates IDs and current era ranges before migration at [plan.md:398](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:398). If version 1 uses tenant ID `office_basic` and version 2 renames it to `office_small`, current-known-ID validation rejects the version-1 save before its migration can translate the ID. Perform only structural and version-specific validation before each migration; apply current-schema validation after the migration chain. Also distinguish rejection from normalization—silently clamping corrupted counts changes data rather than validating it.

- **P2 — CI import handling remains underspecified.** Lines [571–574](/Users/sean/sites/elevator-game-godot/.tmp/ai-review-bb331901/plan.md:571) say to “tolerate only the documented condition,” but identify neither an exit code nor an output pattern for that condition. A `.pck` size floor does not prove imports succeeded; a sufficiently large package can still omit one failed asset. The implementation plan needs the exact shell logic and must fail on an unexplained nonzero import status, with any tolerated status matched to a specific diagnostic.

VERDICT: REVISE
