.godot/
.import/
export.cfg
# export_presets.cfg is tracked: it holds no secrets and CI needs it
*.translation
.DS_Store
build/
export_credentials.cfg
.superpowers/

# Claude Code scratch. NOT a blanket .claude/ ignore: real config there
# (settings.json, hooks, skills) should still show up and be committable.
# These two are machine-local noise -- worktrees/ holds registered git
# worktrees, which git already tracks as branches and must not be committed
# as files.
.claude/worktrees/
.claude/.cc-writes/

# Multi-model review scratch (/debate). Reviewer output and a copy of the diff.
.tmp/

# fastlane artifacts
fastlane/report.xml
fastlane/README.md
